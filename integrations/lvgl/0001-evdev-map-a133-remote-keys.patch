diff --git a/src/drivers/evdev/lv_evdev.c b/src/drivers/evdev/lv_evdev.c
index 5449d82..0824863 100644
--- a/src/drivers/evdev/lv_evdev.c
+++ b/src/drivers/evdev/lv_evdev.c
@@ -104,12 +104,15 @@ static int _evdev_process_key(uint16_t code)
         case KEY_LEFT:
             return LV_KEY_LEFT;
         case KEY_ESC:
+        case KEY_BACK:
             return LV_KEY_ESC;
         case KEY_DELETE:
             return LV_KEY_DEL;
         case KEY_BACKSPACE:
             return LV_KEY_BACKSPACE;
         case KEY_ENTER:
+        case KEY_OK:
+        case KEY_PLAYPAUSE:
             return LV_KEY_ENTER;
         case KEY_NEXT:
         case KEY_TAB:
@@ -117,6 +120,8 @@ static int _evdev_process_key(uint16_t code)
         case KEY_PREVIOUS:
             return LV_KEY_PREV;
         case KEY_HOME:
+        case KEY_MENU:
+        case KEY_PROG1:
             return LV_KEY_HOME;
         case KEY_END:
             return LV_KEY_END;
