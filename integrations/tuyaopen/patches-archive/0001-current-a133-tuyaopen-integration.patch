diff --git a/apps/tuya.ai/your_chat_bot/README_zh.md b/apps/tuya.ai/your_chat_bot/README_zh.md
index 37393779..e86aea2c 100644
--- a/apps/tuya.ai/your_chat_bot/README_zh.md
+++ b/apps/tuya.ai/your_chat_bot/README_zh.md
@@ -3,6 +3,8 @@
 # your_chat_bot
  [your_chat_bot](https://github.com/tuya/TuyaOpen/tree/master/apps/tuya.ai/your_chat_bot) 是基于 tuya.ai 开源的大模型智能聊天机器人。通过麦克风采集语音，语音识别，实现对话、互动、调侃，还能通过屏幕看到实时聊天内容。
 
+A133 Tina SDK 适配、编译、部署和运行说明见：[README_A133_B6.md](./README_A133_B6.md)。
+
 **注意：在 TUYA AI V1.0 和 V2.0 之间切换需要先在 APP 上删除设备并清除数据后才能使用。**
 
 ## 支持功能
@@ -139,4 +141,4 @@
   | 宏                  | 类型 | 说明                        |
   | ------------------- | ---- | --------------------------- |
   | OLED_SSD1306_128X32 | 布尔 | oled 屏幕的尺寸大小为128*32 |
-  | OLED_SSD1306_128X64 | 布尔 | oled 屏幕的尺寸大小为128*64 |
\ No newline at end of file
+  | OLED_SSD1306_128X64 | 布尔 | oled 屏幕的尺寸大小为128*64 |
diff --git a/apps/tuya.ai/your_chat_bot/app_default.config b/apps/tuya.ai/your_chat_bot/app_default.config
index 77e97240..98cee0bc 100644
--- a/apps/tuya.ai/your_chat_bot/app_default.config
+++ b/apps/tuya.ai/your_chat_bot/app_default.config
@@ -1,13 +1,15 @@
 CONFIG_PROJECT_VERSION="1.0.1"
 CONFIG_TUYA_PRODUCT_ID="alon7qgyjj8yus74"
 CONFIG_ENABLE_COMP_AI_AUDIO_CODEC_OPUS=y
-CONFIG_ENABLE_COMP_AI_VIDEO=y
-CONFIG_ENABLE_COMP_AI_PICTURE=y
-CONFIG_ENABLE_AI_UI_TEXT_STREAMING=y
-CONFIG_BUTTON_NAME="ai_chat_button"
-CONFIG_BOARD_CHOICE_T5AI=y
-CONFIG_TUYA_T5AI_BOARD_LCD_35565=y
-CONFIG_TUYA_T5AI_BOARD_CAMERA=y
-CONFIG_AI_INPUT_STACK_SIZE=28672
-CONFIG_ENABLE_MBEDTLS_SSL_MAX_CONTENT_LEN=4096
-CONFIG_ENABLE_LVGL_TP=y
+# CONFIG_ENABLE_COMP_AI_DISPLAY is not set
+CONFIG_ENABLE_UART=y
+CONFIG_KWS_MODEL_PATH="/usr/share/tuyaopen_models/mdtc_chunk_300ms.mnn"
+CONFIG_KWS_MODEL_TOKEN_PATH="/usr/share/tuyaopen_models/tokens.txt"
+# CONFIG_ENABLE_KEYBOARD_INPUT is not set
+CONFIG_BOARD_CHOICE_DshanPI_A1=y
+CONFIG_ENABLE_TUYA_CODEC_OPUS=y
+# CONFIG_ENABLE_TUYA_CODEC_OPUS_IPC is not set
+CONFIG_ENABLE_AUDIO_ALSA=y
+CONFIG_ALSA_DEVICE_CAPTURE="default"
+CONFIG_ALSA_DEVICE_PLAYBACK="default"
+CONFIG_AI_PLAYER_STACK_SIZE=16384
diff --git a/apps/tuya.ai/your_chat_bot/src/app_chat_bot.c b/apps/tuya.ai/your_chat_bot/src/app_chat_bot.c
index 3fc987f6..970b196a 100644
--- a/apps/tuya.ai/your_chat_bot/src/app_chat_bot.c
+++ b/apps/tuya.ai/your_chat_bot/src/app_chat_bot.c
@@ -9,9 +9,15 @@
 
 #include "netmgr.h"
 
+#include "ai_manage_mode.h"
 #include "ai_chat_main.h"
 #include "app_chat_bot.h"
 
+#include <arpa/inet.h>
+#include <stdlib.h>
+#include <sys/socket.h>
+#include <unistd.h>
+
 #if defined(ENABLE_WIFI) && (ENABLE_WIFI == 1)
 #include "tkl_wifi.h"
 #endif
@@ -25,6 +31,8 @@
 ***********************************************************/
 #define PRINTF_FREE_HEAP_TTIME (10 * 1000)
 #define DISP_NET_STATUS_TIME   (1 * 1000)
+#define TUYA_MAIN_UI_PORT      5679
+#define MAIN_UI_REPLY_TEXT_MAX 1024
 
 /***********************************************************
 ***********************typedef define***********************
@@ -38,6 +46,10 @@
 ***********************variable define**********************
 ***********************************************************/
 static TIMER_ID sg_printf_heap_tm;
+static int sg_main_ui_udp_fd = -1;
+static BOOL_T sg_main_ui_udp_close_registered = FALSE;
+static char sg_main_ui_reply_text[MAIN_UI_REPLY_TEXT_MAX];
+static size_t sg_main_ui_reply_len = 0;
 
 #if defined(ENABLE_COMP_AI_DISPLAY) && (ENABLE_COMP_AI_DISPLAY == 1)
 static AI_UI_WIFI_STATUS_E sg_wifi_status = AI_UI_WIFI_STATUS_DISCONNECTED;
@@ -51,6 +63,172 @@ static TIMER_ID            sg_disp_status_tm;
 extern void app_ui_action_register(void);
 #endif
 
+static void __main_ui_bridge_close(void);
+
+static int __main_ui_bridge_send(const char *json)
+{
+    struct sockaddr_in addr;
+    ssize_t sent;
+
+    if (NULL == json) {
+        return -1;
+    }
+
+    if (FALSE == sg_main_ui_udp_close_registered) {
+        atexit(__main_ui_bridge_close);
+        sg_main_ui_udp_close_registered = TRUE;
+    }
+
+    if (sg_main_ui_udp_fd < 0) {
+        sg_main_ui_udp_fd = socket(AF_INET, SOCK_DGRAM, 0);
+        if (sg_main_ui_udp_fd < 0) {
+            PR_WARN("main ui bridge socket create failed");
+            return -1;
+        }
+    }
+
+    memset(&addr, 0, sizeof(addr));
+    addr.sin_family = AF_INET;
+    addr.sin_port = htons(TUYA_MAIN_UI_PORT);
+    inet_pton(AF_INET, "127.0.0.1", &addr.sin_addr);
+
+    sent = sendto(sg_main_ui_udp_fd, json, strlen(json), 0,
+                  (const struct sockaddr *)&addr, sizeof(addr));
+    if (sent < 0) {
+        PR_WARN("main ui bridge send failed");
+        return -1;
+    }
+
+    return 0;
+}
+
+static void __main_ui_bridge_close(void)
+{
+    if (sg_main_ui_udp_fd >= 0) {
+        close(sg_main_ui_udp_fd);
+        sg_main_ui_udp_fd = -1;
+    }
+}
+
+static const char *__main_ui_state_from_mode(AI_MODE_STATE_E state)
+{
+    switch (state) {
+    case AI_MODE_STATE_IDLE:
+    case AI_MODE_STATE_INIT:
+        return "idle";
+    case AI_MODE_STATE_LISTEN:
+        return "listening";
+    case AI_MODE_STATE_UPLOAD:
+    case AI_MODE_STATE_THINK:
+        return "thinking";
+    case AI_MODE_STATE_SPEAK:
+        return "speaking";
+    default:
+        return "error";
+    }
+}
+
+static void __main_ui_bridge_send_state(const char *state)
+{
+    char payload[128];
+
+    snprintf(payload, sizeof(payload),
+             "{\"runtime\":\"tuya\",\"state\":\"%s\"}",
+             state ? state : "error");
+    __main_ui_bridge_send(payload);
+}
+
+static void __json_escape_copy(char *dst, size_t dst_size,
+                               const char *src, size_t src_len)
+{
+    size_t i;
+    size_t n = 0;
+
+    if (!dst || dst_size == 0) {
+        return;
+    }
+
+    for (i = 0; src && i < src_len && src[i] != '\0' && n + 1 < dst_size; i++) {
+        if ((src[i] == '"' || src[i] == '\\') && n + 2 < dst_size) {
+            dst[n++] = '\\';
+            dst[n++] = src[i];
+        } else if ((unsigned char)src[i] >= 0x20) {
+            dst[n++] = src[i];
+        }
+    }
+    dst[n] = '\0';
+}
+
+static void __main_ui_bridge_send_text_data(const char *prefix,
+                                            const char *data, size_t len)
+{
+    char escaped[330];
+    char payload[384];
+
+    if (!data || len == 0) {
+        return;
+    }
+
+    __json_escape_copy(escaped, sizeof(escaped), data, len);
+    snprintf(payload, sizeof(payload),
+             "{\"runtime\":\"tuya\",\"text\":\"%s%s\"}",
+             prefix ? prefix : "", escaped);
+    __main_ui_bridge_send(payload);
+}
+
+static void __main_ui_bridge_send_text(const char *prefix, AI_NOTIFY_TEXT_T *text)
+{
+    if (!text || !text->data || text->datalen == 0) {
+        return;
+    }
+
+    __main_ui_bridge_send_text_data(prefix, text->data, text->datalen);
+}
+
+static BOOL_T __main_ui_text_valid(AI_NOTIFY_TEXT_T *text)
+{
+    return (text && text->data && text->datalen > 0) ? TRUE : FALSE;
+}
+
+static void __main_ui_reply_reset(void)
+{
+    sg_main_ui_reply_len = 0;
+    sg_main_ui_reply_text[0] = '\0';
+}
+
+static void __main_ui_reply_append(AI_NOTIFY_TEXT_T *text)
+{
+    size_t copy_len;
+
+    if (!text || !text->data || text->datalen == 0) {
+        return;
+    }
+
+    if (sg_main_ui_reply_len >= sizeof(sg_main_ui_reply_text) - 1) {
+        return;
+    }
+
+    copy_len = text->datalen;
+    if (copy_len > sizeof(sg_main_ui_reply_text) - 1 - sg_main_ui_reply_len) {
+        copy_len = sizeof(sg_main_ui_reply_text) - 1 - sg_main_ui_reply_len;
+    }
+
+    memcpy(&sg_main_ui_reply_text[sg_main_ui_reply_len], text->data, copy_len);
+    sg_main_ui_reply_len += copy_len;
+    sg_main_ui_reply_text[sg_main_ui_reply_len] = '\0';
+}
+
+static void __main_ui_reply_publish(void)
+{
+    if (sg_main_ui_reply_len == 0) {
+        return;
+    }
+
+    __main_ui_bridge_send_text_data("助手：",
+                                    sg_main_ui_reply_text,
+                                    sg_main_ui_reply_len);
+}
+
 static void __printf_free_heap_tm_cb(TIMER_ID timer_id, void *arg)
 {
 #if defined(ENABLE_EXT_RAM) && (ENABLE_EXT_RAM == 1)
@@ -107,7 +285,55 @@ static void __display_status_tm_cb(TIMER_ID timer_id, void *arg)
 
 static void __ai_chat_handle_event(AI_NOTIFY_EVENT_T *event)
 {
+    AI_NOTIFY_TEXT_T *text = NULL;
+
+    if (NULL == event) {
+        return;
+    }
+
     switch(event->type) {
+        case AI_USER_EVT_MODE_STATE_UPDATE: {
+            AI_MODE_STATE_E state = (AI_MODE_STATE_E)(event->data);
+            __main_ui_bridge_send_state(__main_ui_state_from_mode(state));
+        } break;
+        case AI_USER_EVT_ASR_OK: {
+            text = (AI_NOTIFY_TEXT_T *)event->data;
+            if (!__main_ui_text_valid(text)) {
+                PR_DEBUG("main ui ignore empty ASR");
+                break;
+            }
+            __main_ui_bridge_send_text("我：", text);
+            __main_ui_bridge_send_state("thinking");
+        } break;
+        case AI_USER_EVT_TEXT_STREAM_START: {
+            text = (AI_NOTIFY_TEXT_T *)event->data;
+            __main_ui_reply_reset();
+            __main_ui_reply_append(text);
+            __main_ui_reply_publish();
+        } break;
+        case AI_USER_EVT_TEXT_STREAM_DATA: {
+            text = (AI_NOTIFY_TEXT_T *)event->data;
+            __main_ui_reply_append(text);
+            __main_ui_reply_publish();
+        } break;
+        case AI_USER_EVT_TEXT_STREAM_STOP: {
+            text = (AI_NOTIFY_TEXT_T *)event->data;
+            __main_ui_reply_append(text);
+            __main_ui_reply_publish();
+        } break;
+        case AI_USER_EVT_TEXT_STREAM_ABORT:
+        case AI_USER_EVT_CHAT_BREAK: {
+            __main_ui_reply_reset();
+        } break;
+        case AI_USER_EVT_TTS_PRE:
+        case AI_USER_EVT_TTS_START: {
+            __main_ui_bridge_send_state("speaking");
+        } break;
+        case AI_USER_EVT_TTS_STOP:
+        case AI_USER_EVT_TTS_ABORT:
+        case AI_USER_EVT_TTS_ERROR: {
+            __main_ui_bridge_send_state("idle");
+        } break;
         #if defined(ENABLE_PRINTER) && (ENABLE_PRINTER == 1)
         case AI_USER_EVT_GENERATE_PICTURE:
         case AI_USER_EVT_GET_PICTURE_FROM_APP: {
@@ -127,7 +344,7 @@ OPERATE_RET app_chat_bot_init(void)
     OPERATE_RET rt = OPRT_OK;
 
     AI_CHAT_MODE_CFG_T ai_chat_cfg = {
-        .default_mode = AI_CHAT_MODE_HOLD,
+        .default_mode = AI_CHAT_MODE_WAKEUP,
         .default_vol  = 70,
         .evt_cb       = __ai_chat_handle_event,
     };
@@ -153,7 +370,7 @@ OPERATE_RET app_chat_bot_init(void)
     tal_sw_timer_create(__printf_free_heap_tm_cb, NULL, &sg_printf_heap_tm);
     tal_sw_timer_start(sg_printf_heap_tm, PRINTF_FREE_HEAP_TTIME, TAL_TIMER_CYCLE);
 
-#if defined(ENABLE_COMP_AI_DISPLAY) && (ENABLE_COMP_AI_DISPLAY == 1)
+    #if defined(ENABLE_COMP_AI_DISPLAY) && (ENABLE_COMP_AI_DISPLAY == 1)
     ai_ui_disp_msg(AI_UI_DISP_NETWORK, (uint8_t *)&sg_wifi_status, sizeof(AI_UI_WIFI_STATUS_E));
 
     ai_ui_disp_msg(AI_UI_DISP_STATUS, (uint8_t *)INITIALIZING, strlen(INITIALIZING));
@@ -164,5 +381,7 @@ OPERATE_RET app_chat_bot_init(void)
     tal_sw_timer_start(sg_disp_status_tm, DISP_NET_STATUS_TIME, TAL_TIMER_CYCLE);
 #endif
 
+    __main_ui_bridge_send_state("connecting");
+
     return OPRT_OK;
 }
diff --git a/apps/tuya.ai/your_chat_bot/src/tuya_main.c b/apps/tuya.ai/your_chat_bot/src/tuya_main.c
index 8d56f6f2..82cf7654 100644
--- a/apps/tuya.ai/your_chat_bot/src/tuya_main.c
+++ b/apps/tuya.ai/your_chat_bot/src/tuya_main.c
@@ -17,6 +17,8 @@
 #include "tuya_cloud_types.h"
 
 #include <assert.h>
+#include <stdio.h>
+#include <string.h>
 #include "cJSON.h"
 #include "tal_api.h"
 #include "tuya_config.h"
@@ -72,7 +74,12 @@ tuya_iot_license_t license;
  */
 void user_log_output_cb(const char *str)
 {
-    tal_uart_write(TUYA_UART_NUM_0, (const uint8_t *)str, strlen(str));
+    if (str == NULL) {
+        return;
+    }
+
+    fputs(str, stdout);
+    fflush(stdout);
 }
 
 /**
@@ -177,6 +184,8 @@ void user_event_handler_on(tuya_iot_client_t *client, tuya_event_msg_t *event)
 #if defined(ENABLE_QRCODE) && (ENABLE_QRCODE == 1)
         char buffer[255];
         sprintf(buffer, "https://smartapp.tuya.com/s/p?p=%s&uuid=%s&v=2.0", TUYA_PRODUCT_ID, license.uuid);
+        PR_INFO("Tuya APP bind URL: %s", buffer);
+        PR_INFO("If QR code is not visible, generate a QR code from the URL above.");
         qrcode_string_output(buffer, user_log_output_cb, 0);
 #endif
     } break;
diff --git a/boards/LINUX/DshanPi_A1/Kconfig b/boards/LINUX/DshanPi_A1/Kconfig
index ea39f247..e801468d 100644
--- a/boards/LINUX/DshanPi_A1/Kconfig
+++ b/boards/LINUX/DshanPi_A1/Kconfig
@@ -1,7 +1,7 @@
 menu "DshanPi A1 Board Configuration"
     config CHIP_CHOICE
         string
-        default "DshanPi_A1"
+        default "A133_B6"
 
     config BOARD_CHOICE
         string
diff --git a/src/ai_components/ai_agent/src/ai_agent.c b/src/ai_components/ai_agent/src/ai_agent.c
index 529ebecc..1b05020a 100755
--- a/src/ai_components/ai_agent/src/ai_agent.c
+++ b/src/ai_components/ai_agent/src/ai_agent.c
@@ -48,6 +48,7 @@
 #if defined(ENABLE_COMP_AI_AUDIO) && (ENABLE_COMP_AI_AUDIO == 1)
 static uint16_t __s_audio_codec_type = AI_AUDIO_CODEC_MP3;
 #endif
+static BOOL_T __s_network_connected_alert_played = FALSE;
 /***********************************************************
 ***********************function define**********************
 ***********************************************************/
@@ -175,6 +176,14 @@ OPERATE_RET __ai_agent_alert_cb(AI_ALERT_TYPE_E type)
         return OPRT_OK;
     }
 
+    if (type == AT_NETWORK_CONNECTED) {
+        if (__s_network_connected_alert_played) {
+            PR_DEBUG("ignored reconnect alert: %d", type);
+            return OPRT_OK;
+        }
+        __s_network_connected_alert_played = TRUE;
+    }
+
     ai_user_event_notify(AI_USER_EVT_PLAY_ALERT, (void*)type);   
 
     return OPRT_OK;
@@ -370,4 +379,4 @@ OPERATE_RET ai_agent_role_switch(char *role)
     cJSON_Delete(result);
 
     return OPRT_OK;    
-}
\ No newline at end of file
+}
diff --git a/src/ai_components/ai_main/src/ai_chat_main.c b/src/ai_components/ai_main/src/ai_chat_main.c
index fd759e0d..6550ceab 100755
--- a/src/ai_components/ai_main/src/ai_chat_main.c
+++ b/src/ai_components/ai_main/src/ai_chat_main.c
@@ -393,6 +393,12 @@ OPERATE_RET ai_chat_init(AI_CHAT_MODE_CFG_T *cfg)
         is_need_save = true;
     }
 
+    if (sg_ai_default_mode == AI_CHAT_MODE_WAKEUP && mode == AI_CHAT_MODE_HOLD) {
+        PR_INFO("migrate chat mode from HOLD(0) to WAKEUP(2)");
+        mode = sg_ai_default_mode;
+        is_need_save = true;
+    }
+
     if(is_need_save) {
         __ai_chat_save_config(mode, vol);
     }
@@ -479,4 +485,3 @@ int ai_chat_get_volume(void)
 
     return volume;
 }
-
diff --git a/src/ai_components/ai_mode/src/ai_mode_wakeup.c b/src/ai_components/ai_mode/src/ai_mode_wakeup.c
index 11e92797..1e04190d 100755
--- a/src/ai_components/ai_mode/src/ai_mode_wakeup.c
+++ b/src/ai_components/ai_mode/src/ai_mode_wakeup.c
@@ -60,6 +60,8 @@ static uint32_t        sg_wakeup_time_ms = AI_CHAT_WAKEUP_TIME_MS;
 ***********************************************************/
 static void __ai_mode_kws_wakeup(TKL_KWS_WAKEUP_WORD_E wakeup_word)
 {
+    PR_INFO("KWS wakeup detected: %d", wakeup_word);
+
     ai_audio_player_stop(AI_AUDIO_PLAYER_ALL);
     ai_audio_input_reset();
     tuya_ai_agent_event(AI_EVENT_CHAT_BREAK, 0);
@@ -353,4 +355,4 @@ OPERATE_RET ai_mode_wakeup_register(void)
 {
     return OPRT_NOT_SUPPORTED;
 }
-#endif
\ No newline at end of file
+#endif
diff --git a/src/audio_player/src/svc_ai_player.c b/src/audio_player/src/svc_ai_player.c
index ab9ee728..fad6a7d2 100755
--- a/src/audio_player/src/svc_ai_player.c
+++ b/src/audio_player/src/svc_ai_player.c
@@ -560,23 +560,25 @@ OPERATE_RET tuya_ai_player_service_init(AI_PLAYER_CFG_T *cfg)
 {
     OPERATE_RET rt = OPRT_OK;
 
-    TUYA_CALL_ERR_RETURN(tal_queue_create_init(&s_ai_player_ctx.queue, SIZEOF(AI_PLAYER_MSG_T), 2));
+    TUYA_CHECK_NULL_RETURN(cfg, OPRT_INVALID_PARM);
 
-    THREAD_CFG_T thread_cfg = {0};
-    thread_cfg.stackDepth = AI_PLAYER_STACK_SIZE;
-    thread_cfg.priority   = THREAD_PRIO_1;
-    thread_cfg.thrdname   = "ai_player";
-    thread_cfg.psram_mode = 1;
-    TUYA_CALL_ERR_RETURN(tal_thread_create_and_start(&s_ai_player_ctx.thread, NULL, NULL, __ai_player_thread_cb, NULL, &thread_cfg));
+    TUYA_CALL_ERR_RETURN(tal_queue_create_init(&s_ai_player_ctx.queue, SIZEOF(AI_PLAYER_MSG_T), 2));
 
     memcpy(&s_ai_player_ctx.cfg, cfg, SIZEOF(AI_PLAYER_CFG_T));
     memcpy(&s_ai_player_ctx.consumer, &g_consumer_speaker, SIZEOF(AI_PLAYER_CONSUMER_T));
-    g_consumer_speaker.open(&s_ai_player_ctx.consumer_handle);
+    TUYA_CALL_ERR_RETURN(g_consumer_speaker.open(&s_ai_player_ctx.consumer_handle));
     ai_player_resample_init(cfg->sample, cfg->datebits, cfg->channel);
     s_ai_player_ctx.decoder_mode = true;
     s_ai_player_ctx.volume = PLAYER_MAX_VOLUME;
     s_ai_player_ctx.mute = false;
 
+    THREAD_CFG_T thread_cfg = {0};
+    thread_cfg.stackDepth = AI_PLAYER_STACK_SIZE;
+    thread_cfg.priority   = THREAD_PRIO_1;
+    thread_cfg.thrdname   = "ai_player";
+    thread_cfg.psram_mode = 1;
+    TUYA_CALL_ERR_RETURN(tal_thread_create_and_start(&s_ai_player_ctx.thread, NULL, NULL, __ai_player_thread_cb, NULL, &thread_cfg));
+
     return rt;
 }
 
