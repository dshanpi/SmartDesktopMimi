diff --git a/CMakeLists.txt b/CMakeLists.txt
index 6fca361..3a4d48e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -46,6 +46,10 @@ if(OPEN_HEADER_DIR)
 endif()
 
 add_executable(${CONFIG_PROJECT_NAME} ${SOURCES})
+set_target_properties(${CONFIG_PROJECT_NAME} PROPERTIES
+    BUILD_RPATH "$ORIGIN/lib;$ORIGIN"
+    INSTALL_RPATH "$ORIGIN/lib;$ORIGIN"
+)
 
 # Set target-specific compile options based on build type
 if(CMAKE_BUILD_TYPE STREQUAL "Debug")
@@ -66,7 +70,7 @@ endif()
 
 list(APPEND TUYAOPEN_FOUND_LIBRARIES "pthread")
 
-if(NOT DEFINED CONFIG_COMPILE_PREX OR "${CONFIG_COMPILE_PREX}" STREQUAL "")
+if((NOT DEFINED CONFIG_COMPILE_PREX OR "${CONFIG_COMPILE_PREX}" STREQUAL "") AND NOT IS_CROSS_COMPILE)
     list(APPEND TUYAOPEN_FOUND_LIBRARIES "systemd")
 endif()
 
diff --git a/compiler_setup.cmake b/compiler_setup.cmake
index 5825d27..000a4f7 100644
--- a/compiler_setup.cmake
+++ b/compiler_setup.cmake
@@ -1,82 +1,20 @@
-# have COMPILE_PREX
-if (NOT DEFINED CONFIG_COMPILE_PREX)
-    set(CONFIG_COMPILE_PREX "aarch64-none-linux-gnu-")
+# Auto-replaced by build_a133.sh — A133 Tina SDK 工具链适配
+# 强制从环境变量读取编译器
+if(NOT DEFINED ENV{CC} OR NOT DEFINED ENV{CXX})
+    message(FATAL_ERROR "A133 build requires CC and CXX env vars (aarch64-openwrt-linux-gnu-gcc/g++)")
 endif()
+set(CMAKE_C_COMPILER "$ENV{CC}")
+set(CMAKE_CXX_COMPILER "$ENV{CXX}")
+set(CMAKE_ASM_COMPILER "$ENV{CC}")
+set(CMAKE_AR "$ENV{AR}")
+set(CMAKE_RANLIB "$ENV{RANLIB}")
+set(CMAKE_STRIP "$ENV{STRIP}")
+set(CMAKE_SYSTEM_NAME Linux)
+set(CMAKE_SYSTEM_PROCESSOR aarch64)
+set(IS_CROSS_COMPILE TRUE)
 
-# Is cross compile?
-if ("${CMAKE_HOST_SYSTEM_NAME}" STREQUAL "Linux")
-    if (EXISTS "/sys/firmware/devicetree/base/model")
-        file(READ "/sys/firmware/devicetree/base/model" DEVICE_MODEL)
-        string(TOLOWER "${DEVICE_MODEL}" DEVICE_MODEL_LOWER)
-        # DshanPi_A1
-        if (DEVICE_MODEL_LOWER MATCHES "dshanpi a1")
-            set(IS_CROSS_COMPILE FALSE)
-        else()
-            set(IS_CROSS_COMPILE TRUE)
-        endif()
-        # Raspberry_Pi
-        if (DEVICE_MODEL_LOWER MATCHES "raspberry pi")
-            set(IS_CROSS_COMPILE FALSE)
-        else()
-            set(IS_CROSS_COMPILE TRUE)
-        endif()
-    else()
-        if (PLATFORM_BOARD STREQUAL "Raspberry_Pi")
-            set(IS_CROSS_COMPILE TRUE)
-        elseif (PLATFORM_BOARD STREQUAL "DshanPi_A1")
-            set(IS_CROSS_COMPILE TRUE)
-        else()
-            set(IS_CROSS_COMPILE FALSE)
-        endif()
-    endif()
+if (WIN32)
+    set(EXE_SUFFIX ".exe")
 else()
-    set(IS_CROSS_COMPILE TRUE)
-endif()
-
-# Toolchain path
-get_filename_component(TOOLCHAIN_PATH "${CMAKE_CURRENT_LIST_DIR}/../tools/" ABSOLUTE)
-
-if (IS_CROSS_COMPILE)
-    set(TOOLCHAIN_PREFIX "")
-    if (PLATFORM_BOARD STREQUAL "Raspberry_Pi")
-        get_filename_component(TOOLCHAIN_PREFIX "${TOOLCHAIN_PATH}/aarch64-none-linux-gnu-14.2-2024.10/bin" ABSOLUTE)
-        # TOOLCHAIN_PREFIX Check
-        if(NOT EXISTS "${TOOLCHAIN_PREFIX}")
-            message(FATAL_ERROR "Toolchain directory does not exist: ${TOOLCHAIN_PREFIX}")
-        endif()
-
-        get_filename_component(TOOLCHAIN_PREFIX "${TOOLCHAIN_PREFIX}/${CONFIG_COMPILE_PREX}" ABSOLUTE)
-    elseif (PLATFORM_BOARD STREQUAL "DshanPi_A1")
-        get_filename_component(TOOLCHAIN_PREFIX "${TOOLCHAIN_PATH}/aarch64-none-linux-gnu-13.3-2024.04/bin" ABSOLUTE)
-        # TOOLCHAIN_PREFIX Check
-        if(NOT EXISTS "${TOOLCHAIN_PREFIX}")
-            message(FATAL_ERROR "Toolchain directory does not exist: ${TOOLCHAIN_PREFIX}")
-        endif()
-
-        get_filename_component(TOOLCHAIN_PREFIX "${TOOLCHAIN_PREFIX}/${CONFIG_COMPILE_PREX}" ABSOLUTE)
-    else()
-        message(FATAL_ERROR "Unsupported PLATFORM_BOARD for cross compilation: ${PLATFORM_BOARD}")
-    endif()
-
-    # On Windows, add .exe suffix to executables
-    if(WIN32)
-        set(EXE_SUFFIX ".exe")
-    else()
-        set(EXE_SUFFIX "")
-    endif()
-
-    set(CMAKE_C_COMPILER "${TOOLCHAIN_PREFIX}gcc${EXE_SUFFIX}")
-    set(CMAKE_CXX_COMPILER "${TOOLCHAIN_PREFIX}g++${EXE_SUFFIX}")
-    set(CMAKE_ASM_COMPILER "${TOOLCHAIN_PREFIX}gcc${EXE_SUFFIX}")
-    set(CMAKE_AR "${TOOLCHAIN_PREFIX}ar${EXE_SUFFIX}")
-    set(CMAKE_RANLIB "${TOOLCHAIN_PREFIX}ranlib${EXE_SUFFIX}")
-    set(CMAKE_STRIP "${TOOLCHAIN_PREFIX}strip${EXE_SUFFIX}")
-else()
-    # Native compile settings
-    set(CMAKE_C_COMPILER "gcc")
-    set(CMAKE_CXX_COMPILER "g++")
-    set(CMAKE_ASM_COMPILER "gcc")
-    set(CMAKE_AR "ar")
-    set(CMAKE_RANLIB "ranlib")
-    set(CMAKE_STRIP "strip")
+    set(EXE_SUFFIX "")
 endif()
diff --git a/platform_prepare.py b/platform_prepare.py
index 47b890c..b24a591 100644
--- a/platform_prepare.py
+++ b/platform_prepare.py
@@ -341,6 +341,11 @@ def prepare_dshanpi_a1():
     toolchain_root = get_toolchain_root()
     return download_and_setup_toolchain(toolchain_root, AARCH64_13_3_TOOLCHAIN_CONFIGS)
 
+def prepare_a133_b6():
+    """Prepare A133 B6 platform"""
+    print("A133_B6 uses Tina SDK toolchain from the environment")
+    return True
+
 def prepare_ubuntu():
     """Prepare Ubuntu platform"""
     print("Ubuntu platform preparation complete")
@@ -362,6 +367,7 @@ def main():
     platform_handlers = {
         "Raspberry_Pi": prepare_raspberry_pi,
         "DshanPi_A1": prepare_dshanpi_a1,
+        "A133_B6": prepare_a133_b6,
         "Ubuntu": prepare_ubuntu,
     }
 
diff --git a/tuyaos_adapter/src/mvec_impl.c b/tuyaos_adapter/src/mvec_impl.c
index 9509767..1558ffd 100644
--- a/tuyaos_adapter/src/mvec_impl.c
+++ b/tuyaos_adapter/src/mvec_impl.c
@@ -13,15 +13,15 @@
 
 #if defined(__aarch64__) || defined(__ARM_ARCH_ISA_A64)
 
-// Types and declarations are already in bits/math-vector.h (included via math.h)
-// We only provide the implementation here
+typedef float tuya_f32x4_t __attribute__((vector_size(16)));
+typedef double tuya_f64x2_t __attribute__((vector_size(16)));
 
 // Single precision (float) vector functions - 4 elements
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_erfcf(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_erfcf(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = erfcf(px[0]);
     pr[1] = erfcf(px[1]);
@@ -31,9 +31,9 @@ __f32x4_t _ZGVnN4v_erfcf(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_erff(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_erff(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = erff(px[0]);
     pr[1] = erff(px[1]);
@@ -43,9 +43,9 @@ __f32x4_t _ZGVnN4v_erff(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_coshf(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_coshf(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = coshf(px[0]);
     pr[1] = coshf(px[1]);
@@ -55,9 +55,9 @@ __f32x4_t _ZGVnN4v_coshf(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_atanhf(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_atanhf(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = atanhf(px[0]);
     pr[1] = atanhf(px[1]);
@@ -67,9 +67,9 @@ __f32x4_t _ZGVnN4v_atanhf(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_asinhf(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_asinhf(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = asinhf(px[0]);
     pr[1] = asinhf(px[1]);
@@ -79,9 +79,9 @@ __f32x4_t _ZGVnN4v_asinhf(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_sinhf(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_sinhf(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = sinhf(px[0]);
     pr[1] = sinhf(px[1]);
@@ -91,9 +91,9 @@ __f32x4_t _ZGVnN4v_sinhf(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4v_acoshf(__f32x4_t x) {
+tuya_f32x4_t _ZGVnN4v_acoshf(tuya_f32x4_t x) {
     float *px = (float*)&x;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = acoshf(px[0]);
     pr[1] = acoshf(px[1]);
@@ -103,10 +103,10 @@ __f32x4_t _ZGVnN4v_acoshf(__f32x4_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f32x4_t _ZGVnN4vv_powf(__f32x4_t x, __f32x4_t y) {
+tuya_f32x4_t _ZGVnN4vv_powf(tuya_f32x4_t x, tuya_f32x4_t y) {
     float *px = (float*)&x;
     float *py = (float*)&y;
-    __f32x4_t result;
+    tuya_f32x4_t result;
     float *pr = (float*)&result;
     pr[0] = powf(px[0], py[0]);
     pr[1] = powf(px[1], py[1]);
@@ -118,9 +118,9 @@ __f32x4_t _ZGVnN4vv_powf(__f32x4_t x, __f32x4_t y) {
 // Double precision (double) vector functions - 2 elements
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f64x2_t _ZGVnN2v_erf(__f64x2_t x) {
+tuya_f64x2_t _ZGVnN2v_erf(tuya_f64x2_t x) {
     double *px = (double*)&x;
-    __f64x2_t result;
+    tuya_f64x2_t result;
     double *pr = (double*)&result;
     pr[0] = erf(px[0]);
     pr[1] = erf(px[1]);
@@ -128,10 +128,10 @@ __f64x2_t _ZGVnN2v_erf(__f64x2_t x) {
 }
 
 __attribute__((__aarch64_vector_pcs__)) 
-__f64x2_t _ZGVnN2vv_pow(__f64x2_t x, __f64x2_t y) {
+tuya_f64x2_t _ZGVnN2vv_pow(tuya_f64x2_t x, tuya_f64x2_t y) {
     double *px = (double*)&x;
     double *py = (double*)&y;
-    __f64x2_t result;
+    tuya_f64x2_t result;
     double *pr = (double*)&result;
     pr[0] = pow(px[0], py[0]);
     pr[1] = pow(px[1], py[1]);
diff --git a/tuyaos_adapter/src/tkl_spi.c b/tuyaos_adapter/src/tkl_spi.c
index 958dc91..5897d62 100644
--- a/tuyaos_adapter/src/tkl_spi.c
+++ b/tuyaos_adapter/src/tkl_spi.c
@@ -32,6 +32,9 @@ typedef uint16_t __u16;
 #ifndef __u32
 typedef uint32_t __u32;
 #endif
+#ifndef __u64
+typedef uint64_t __u64;
+#endif
 
 #define SPI_CPHA 0x01
 #define SPI_CPOL 0x02
diff --git a/tuyaos_adapter/tuyaopen_adapter.cmake b/tuyaos_adapter/tuyaopen_adapter.cmake
index 7112926..b8614bf 100644
--- a/tuyaos_adapter/tuyaopen_adapter.cmake
+++ b/tuyaos_adapter/tuyaopen_adapter.cmake
@@ -233,7 +233,11 @@ if(CONFIG_ENABLE_AUDIO)
 
     ## alsa library (for Raspberry Pi)
     if (IS_CROSS_COMPILE)
-        collect_library("${AUDIO_LIB_PATH}/alsa" "asound")
+        if(PLATFORM_CHIP STREQUAL "A133_B6" AND DEFINED ENV{STAGING_DIR} AND EXISTS "$ENV{STAGING_DIR}/target/usr/lib/libasound.so")
+            collect_library("$ENV{STAGING_DIR}/target/usr/lib" "asound")
+        else()
+            collect_library("${AUDIO_LIB_PATH}/alsa" "asound")
+        endif()
         include_directories("${AUDIO_LIB_PATH}/alsa/include")
     else()
         # For native builds, use system ALSA library
@@ -261,4 +265,3 @@ if(CONFIG_ENABLE_AUDIO)
         endif()
     endif()
 endif()
-
